# 💰 简易月度收支仪表盘 (Expense Dashboard)

> 一个极简、朴实且功能完整的个人月度收支记录工具（全栈版）。

![Project Status](https://img.shields.io/badge/status-active-success.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

## 📖 项目介绍

本项目是一个专为学生和上班族设计的轻量级记账工具。旨在通过极简的 UI，帮助用户快速记录和回顾每月的收支情况。

不同于市面上功能繁杂的记账 App，本项目专注于核心需求：**快速记账**、**一目了然的统计**以及**多端云同步**。

本项目已从纯前端架构升级为**全栈架构 (MEVN Stack)**，支持用户注册登录，数据存储于云端 MongoDB，随时随地访问。

## 🛠️ 技术栈

本项目采用现代前后端分离架构构建：

### 前端 (Frontend)
- **核心框架**: [Vue 3](https://vuejs.org/) (Composition API + Setup语法糖)
- **构建工具**: [Vite](https://vitejs.dev/)
- **路由管理**: [Vue Router](https://router.vuejs.org/) (页面路由与权限控制)
- **状态管理**: [Pinia](https://pinia.vuejs.org/) (管理全局状态与 API 交互)
- **样式方案**: [Tailwind CSS](https://tailwindcss.com/) (Mobile-First 响应式设计)
- **HTTP请求**: [Axios](https://axios-http.com/)

### 后端 (Backend)
- **运行环境**: [Node.js](https://nodejs.org/) (Vercel Serverless Functions)
- **Web框架**: [Express](https://expressjs.com/)
- **数据库**: [MongoDB Atlas](https://www.mongodb.com/atlas) (云数据库)
- **ORM框架**: [Mongoose](https://mongoosejs.com/)
- **认证安全**: [JWT](https://jwt.io/) (Token认证) + [Bcrypt](https://www.npmjs.com/package/bcrypt) (密码加密)

## ✨ 核心功能

1.  **🔐 用户认证系统**
    *   支持用户注册与登录。
    *   基于 JWT (JSON Web Token) 的安全认证机制。
    *   路由守卫保护，未登录无法访问核心功能。

2.  **📊 实时仪表盘**
    *   实时展示本月总收入、总支出及结余。
    *   关键数据大字号显示，财务状况一目了然。

3.  **📝 极简记账**
    *   支持快速录入金额、日期、类型（收入/支出）及备注。
    *   智能默认填充日期，提升记账效率。

4.  **🔍 智能筛选与搜索**
    *   **月份切换**：支持查看历史月份的收支记录。
    *   **关键词搜索**：通过备注关键词（如“餐饮”、“交通”）快速查找特定记录。

5.  **☁️ 云端数据同步**
    *   数据存储于 MongoDB Atlas 云数据库，不再丢失。
    *   支持多设备登录，数据实时同步。

6.  **📱 响应式设计**
    *   完美适配 PC 端与移动端，随时随地均可使用。

## 🚀 快速开始

### 环境要求

- Node.js >= 16.0.0
- npm >= 7.0.0
- MongoDB Atlas 账号 (获取连接字符串)

### 安装步骤

1.  **克隆仓库**

    ```bash
    git clone https://github.com/Fogu0208/Expense-Dashboard.git
    cd expense-dashboard
    ```

2.  **安装依赖**

    ```bash
    npm install
    ```

3.  **启动开发环境**

    本项目配置了前后端连调环境。

    *   **启动后端 API** (默认端口 3000):
        ```bash
        node api/index.js
        ```

    *   **启动前端** (另开一个终端):
        ```bash
        npm run dev
        ```

    打开浏览器访问 `http://localhost:5173` 即可。

4.  **部署**

    本项目已配置 `vercel.json`，支持一键部署到 Vercel。
    *   前端构建命令: `npm run build`
    *   后端: 自动识别为 Serverless Functions

## 📂 目录结构

```text
root/
├── api/                  # 后端代码 (Express)
│   ├── models/           # Mongoose 数据模型 (User, Expense)
│   └── index.js          # 后端入口与路由定义
├── src/                  # 前端代码 (Vue 3)
│   ├── components/       # UI 组件 (表单, 列表, 统计卡片)
│   ├── router/           # 路由配置
│   ├── stores/           # Pinia 状态管理 (API调用)
│   ├── views/            # 页面视图 (Login, Register, Dashboard)
│   ├── App.vue           # 根组件
│   └── main.ts           # 前端入口
├── vercel.json           # Vercel 部署配置
└── vite.config.ts        # Vite 配置 (包含 API 代理)
```

## 📄 许可证

本项目采用 MIT 许可证。
