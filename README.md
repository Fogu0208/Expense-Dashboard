# 💰 简易月度收支仪表盘 (Expense Dashboard)

> 一个极简、朴实且功能完整的个人月度收支记录与可视化工具。

![Project Status](https://img.shields.io/badge/status-active-success.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

## 📖 项目介绍

本项目是一个专为学生和上班族设计的轻量级记账工具。旨在通过极简的 UI 和直观的数据可视化，帮助用户快速记录和回顾每月的收支情况。

不同于市面上功能繁杂的记账 App，本项目**拒绝花里胡哨**，专注于核心需求：**快速记账**、**一目了然的统计**以及**数据安全（纯本地存储）**。

## 🛠️ 技术栈

本项目采用现代前端技术栈构建，注重代码规范与开发体验：

- **核心框架**: [Vue 3](https://vuejs.org/) (Composition API + Setup语法糖)
- **构建工具**: [Vite](https://vitejs.dev/) (秒级启动与热更新)
- **编程语言**: [TypeScript](https://www.typescriptlang.org/) (全类型覆盖，保障代码健壮性)
- **样式方案**: [Tailwind CSS](https://tailwindcss.com/) (原子化 CSS，实现极简响应式设计)
- **状态管理**: [Pinia](https://pinia.vuejs.org/) (轻量级状态管理，结合 LocalStorage 持久化)
- **图表库**: [Chart.js](https://www.chartjs.org/) + [vue-chartjs](https://vue-chartjs.org/) (直观的数据可视化)

## ✨ 核心功能

1.  **📊 实时仪表盘**
    *   实时展示本月总收入、总支出及结余。
    *   关键数据大字号显示，财务状况一目了然。

2.  **📝 极简记账**
    *   支持快速录入金额、日期、类型（收入/支出）及备注。
    *   智能默认填充日期，提升记账效率。

3.  **📈 数据可视化**
    *   自动生成「支出构成」饼图。
    *   按备注类别自动聚合数据，直观展示消费去向。

4.  **🔍 智能筛选与搜索**
    *   **月份切换**：支持查看历史月份的收支记录。
    *   **关键词搜索**：通过备注关键词（如“餐饮”、“交通”）快速查找特定记录。

5.  **💾 数据持久化**
    *   基于浏览器的 `localStorage` 进行本地存储。
    *   无需注册登录，数据完全掌握在用户自己手中，刷新或关闭浏览器不丢失。

6.  **📱 响应式设计**
    *   完美适配 PC 端与移动端，随时随地均可使用。

## 🚀 快速开始

### 环境要求

- Node.js >= 16.0.0
- npm >= 7.0.0

### 安装步骤

1.  **克隆仓库**

    ```bash
    git clone https://github.com/Fogu0208/Expense-Dashboard.git
    cd expense-dashboard
    ```

2.  **安装依赖**

    ```bash
    npm install
    ```

3.  **启动开发服务器**

    ```bash
    npm run dev
    ```

    打开浏览器访问 `http://localhost:5173` 即可看到效果。

4.  **构建生产版本**

    ```bash
    npm run build
    ```

## 📂 目录结构

```text
src/
├── components/        # UI 组件
│   ├── AddRecordForm.vue   # 记账表单
│   ├── DashboardStats.vue  # 统计卡片
│   ├── ExpenseChart.vue    # 可视化图表
│   └── ExpenseList.vue     # 收支列表
├── stores/           # 状态管理 (Pinia)
│   └── expense.ts          # 核心收支逻辑与持久化
├── types/            # TypeScript 类型定义
├── App.vue           # 根组件 (布局与路由)
├── main.ts           # 入口文件
└── style.css         # 全局样式 (Tailwind)
```

## 📄 许可证

本项目采用 MIT 许可证。
